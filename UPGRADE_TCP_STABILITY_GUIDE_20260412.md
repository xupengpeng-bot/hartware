# OTA/TCP 稳定性联调说明

更新时间：2026-04-12

## 结论

当前问题更接近“设备侧大帧接收链路对 `upg` 不够稳”，不是“平台完全没投递”，也不是“设备完全不回”。

现象上已经确认：

- 查询类 `qgs` 可以到板并返回 `QS`
- `upg` 大帧已经到达 `QIRD` 原始收包层
- 连接存在中途关闭和重连
- `upg` 在部分日志中只看到 partial chunk，没有稳定进入解析层并产出 `AK/NK`

## 对服务端的明确要求

### 1. 以业务 ACK 为准，不以 TCP 写成功为准

服务端发送 `qgs/qgc/upg` 后，只有收到对应业务回包才算成功：

- 查询：`QS`
- 执行：`AK` 或 `NK`

只要没有收到业务回包，就只能认为“结果未知”，不能认为“设备已收到并处理完成”。

### 2. 重试必须复用同一个 `c`

同一条业务命令发生超时重试时：

- 必须复用同一个相关键 `c`
- 不要每次重试生成新的命令 ID
- `upg` 还应复用同一个 `upgrade_ticket`

原因：

- 设备侧需要把重试识别为同一业务命令，而不是新的独立命令
- 这样才能做幂等 ACK 和状态对齐

### 3. `upg` 超时重试时，不要改 token

若 `upg` 已被设备接受，但第一次 `AK` 在链路中丢失：

- 服务端应使用相同 `upgrade_ticket`
- 设备现在会把“同 token 且已进入 OTA 流程”的重试识别为幂等重放，并返回 `AK`

不要做的事：

- 不要换新的 `upgrade_ticket`
- 不要把同一个升级任务拆成多个逻辑命令并行推送

### 4. 设备重连后，等待连接稳定再发大命令

服务端发现设备刚刚重连时：

- 先等待设备重新 `RG/RA` 或至少一个稳定心跳
- 再发送 `upg`

不要在这些时间点立即推 `upg`：

- 新 TCP 连接刚建立后的瞬间
- 设备刚出现 `closed`/`recv` 抖动后的 1 到 2 个周期内
- 设备刚完成注册但还没进入稳定心跳窗口时

### 5. 同设备只保留一个有效连接

服务端必须保证：

- 同一设备只有一个活跃 TCP 会话被视为有效
- 新连接建立后，旧连接要立即摘除
- 不允许两个连接同时向同一设备发命令

否则容易出现：

- 命令发到旧连接
- 业务回包回到新连接
- 平台状态错乱

### 6. 大命令发送后保留足够回包等待窗口

发送 `upg` 后，服务端不应快速判断失败并切连接，至少要留出完整的业务等待窗口。

建议：

- `upg` 的业务等待超时显著高于 `qgs/qgc`
- 超时后先查设备状态，再决定是否重发

### 7. 超时后优先查状态，不要直接重复下发新升级任务

推荐顺序：

1. `upg` 超时未见 `AK/NK`
2. 先发 `qgs` 或专用升级状态查询
3. 若设备已进入下载/校验/写入阶段，则不要再创建新升级任务
4. 若设备确实仍未进入 OTA，再按原 `c` 和原 `upgrade_ticket` 重试

## 设备侧本次已补的稳态能力

- `QIRD` 单次抓取上限已从 `256` 提高到 `768`
- TCP staging FIFO 已从 `768` 提高到 `1536`
- 新增 FIFO 覆盖告警，能看到是否发生设备侧缓存顶掉
- 新增 `QIRD header/result/pending` 诊断日志
- 新增协议层 `partial_frame/frame_ready` 日志
- `upg` 同 token 且已进入 OTA 流程时，重试将走幂等 ACK，而不是直接 `NK duplicate`

## 下一轮日志重点

请重点关注以下日志：

- `[4G] QIRD header ...`
- `[4G] QIRD result tag=partial/complete ...`
- `[4G] QIRD pending fetch start ...`
- `[PROTO] RX partial_frame ...`
- `[PROTO] RX frame_ready ...`
- `[4G] TCP RX fifo overwrite ...`

如果 `upg` 还是失败，这几类日志将直接告诉我们问题落在哪一层：

- `QIRD` 没继续补
- 已补齐但协议层没拼成整帧
- 整帧拼成后解析失败
- 缓存被覆盖
- 连接在补包前后被掐断
